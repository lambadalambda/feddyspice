# Standalone deployment (Docker Compose)
#
# Copy this file to docker/standalone/.env (gitignored) and adjust.

# Public domain and scheme feddyspice will use when generating ActivityPub URLs.
FEDDYSPICE_DOMAIN=example.com
FEDDYSPICE_SCHEME=https

# Email for Let's Encrypt (optional but recommended).
CADDY_EMAIL=you@example.com

# Optional file logging (otherwise use `docker compose logs`).
FEDDYSPICE_LOG_LEVEL=info
FEDDYSPICE_LOG_FILE=/data/feddyspice.log

# Background jobs (default: spawn).
FEDDYSPICE_JOBS_MODE=spawn

